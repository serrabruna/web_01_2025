function multiplicar(a: number, b: number): number{
    let mult: number = a * b;
    return mult;
}

console.log(multiplicar(2, 7));

const multiArrow = (a: number, b:number): void => console.log(a * b);

multiArrow(2,3);
